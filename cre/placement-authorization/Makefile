.PHONY: build

# CRE invokes `make build` for custom WASM workflows. The deploy and local
# verification steps compile this artifact before any simulation request.
build:
	@test -s wasm/workflow.wasm
